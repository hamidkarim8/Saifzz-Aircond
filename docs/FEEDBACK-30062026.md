# Feedback — Saifzz Aircond (30 Jun 2026)

**For Khalid:** Review each item below. Change `TESTING` → `CLOSED` for anything that looks good. Use the Notes column for anything that needs adjustment or re-work.

**Status flow:** `OPEN` → `TESTING` (deployed, awaiting check) → `CLOSED` (confirmed ok)
**Bug severity:** `Critical` core broken · `High` major feature broken · `Medium` annoying · `Low` cosmetic
**Change/Feature priority:** `P1` blocking · `P2` important · `P3` nice to have

**Summary:** 1 bug = **1 item total**

---

## Bugs

| ID      | Status | Severity | Area         | Description | Notes |
| ------- | ------ | -------- | ------------ | ----------- | ----- |
| BUG-001 | CLOSED | Critical | Service Fees | Editing any service fee caused a 500 error | PostgreSQL sequence for `service_fees` was out of sync with actual row IDs — caused by old-VM data being restored with explicit IDs that bypassed the sequence. Fixed directly on prod: `SELECT setval('service_fees_id_seq', (SELECT COALESCE(MAX(id), 0) FROM service_fees))`. No code change needed. |
