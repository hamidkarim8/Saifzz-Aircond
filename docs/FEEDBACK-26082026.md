# Feedback — Saifzz Aircond (26 Aug 2026)

**For Khalid:** Review each item below. Change `TESTING` → `CLOSED` for anything that looks good. Use the Notes column for anything that needs adjustment or re-work.

**Status flow:** `OPEN` → `TESTING` (deployed, awaiting check) → `CLOSED` (confirmed ok)
**Bug severity:** `Critical` core broken · `High` major feature broken · `Medium` annoying · `Low` cosmetic
**Change/Feature priority:** `P1` blocking · `P2` important · `P3` nice to have

**Summary:** 2 bugs = **2 items total**

---

## Bugs

| ID      | Status  | Severity | Area              | Description | Notes |
| ------- | ------- | -------- | ----------------- | ----------- | ----- |
| BUG-013 | TESTING | High     | Business Settings | Uploading a new QR image did nothing — the page said "Business settings saved" but the old QR stayed. Affected both the Manual QR (Payment tab) and the Google Review QR. | The image never reached the server: the browser sent the upload as a PUT request, and PHP does not read attached files on PUT. Both upload forms now send it in a way PHP accepts. Text-only fields (name, phone, WhatsApp, credentials) were never affected. |
| BUG-014 | TESTING | Medium   | Payments / Records | After replacing a QR, the old image could still show on the payment-collection page and the new-service-record page, even once the upload itself worked. | Every QR is saved under the same filename, so the browser kept serving its cached copy. All pages showing a QR now request it with a version stamp taken from the file, so a replacement appears immediately. |
