# Feedback — Saifzz Aircond (2 Jul 2026)

**For Khalid:** Review each item below. Change `TESTING` → `CLOSED` for anything that looks good. Use the Notes column for anything that needs adjustment or re-work.

**Status flow:** `OPEN` → `TESTING` (deployed, awaiting check) → `CLOSED` (confirmed ok)
**Bug severity:** `Critical` core broken · `High` major feature broken · `Medium` annoying · `Low` cosmetic
**Change/Feature priority:** `P1` blocking · `P2` important · `P3` nice to have

**Summary:** 1 bug + 2 features + 2 changes = **5 items total**

---

## Bugs

| ID      | Status  | Severity | Area         | Description | Notes |
| ------- | ------- | -------- | ------------ | ----------- | ----- |
| BUG-002 | TESTING | Low      | Transactions | Date-range filter (CHG-025) badly positioned on mobile/PWA | Period chips + date-range picker were crammed into the sticky top header, which has a fixed height — on narrow screens the content wrapped and got clipped/overlapped by the bell/user icons. Moved into a normal-flow filter row above the stat cards, matching the pattern already used on Service Records. |

## Features

| ID       | Status | Priority | Area           | Description | Notes |
| -------- | ------ | -------- | -------------- | ----------- | ----- |
| FEAT-019 | TESTING | P1       | Service Records | Void a paid service record (Cancel already exists for pending) | Triggered by 1-Jul incident — Khalid mistakenly created + fully paid a record. Void keeps the record + invoice/receipt in DB for audit but removes it from customer portal view; requires typed reason; reverts linked appointment if auto-completed. Design: `docs/superpowers/specs/2026-07-02-void-paid-service-record-design.md` |
| FEAT-020 | TESTING | P2       | Service Records | Set/correct next service date on a paid record | Khalid often forgot to pick the next-service month at creation; once paid it was locked out with no way to fix it. New inline "Edit" control on the paid record's service line lets him set or correct it anytime, and it resyncs the unit's next-service date automatically. Design: `docs/superpowers/specs/2026-07-02-paid-record-next-service-date-design.md` |

## Changes

| ID      | Status | Priority | Area             | Description | Notes |
| ------- | ------ | -------- | ---------------- | ----------- | ----- |
| CHG-024 | TESTING | P2       | Service Records  | Status chip filter on Service Records list (All / Paid / Pending / Cancelled / Void) | Default All. Mirrors existing Transactions status-chip pattern. |
| CHG-025 | TESTING | P2       | Transactions     | Custom date-range filter (From/To) on Transactions list | Sits beside existing Today/Week/Month/All chips; picking a range deselects the chips and overrides them. |
