# Feedback — Saifzz Aircond (8 Jul 2026)

**For Khalid:** Review each item below. Change `TESTING` → `CLOSED` for anything that looks good. Use the Notes column for anything that needs adjustment or re-work.

**Status flow:** `OPEN` → `TESTING` (deployed, awaiting check) → `CLOSED` (confirmed ok)
**Bug severity:** `Critical` core broken · `High` major feature broken · `Medium` annoying · `Low` cosmetic
**Change/Feature priority:** `P1` blocking · `P2` important · `P3` nice to have

**Summary:** 1 bug = **1 item total**

---

## Bugs

| ID      | Status  | Severity | Area      | Description | Notes |
| ------- | ------- | -------- | --------- | ----------- | ----- |
| BUG-008 | TESTING | High     | Dashboard | "Service by Type" chart counted every service line for visits in the period, regardless of payment status — pending, failed, and voided transactions (and visits with no transaction at all) were all included, inflating the counts. Query now joins `transactions` and filters `status = 'paid'`, matching the revenue KPI's definition of "paid". | Root cause: `ReportService::servicesByType()` never touched the `transactions` table at all. |
